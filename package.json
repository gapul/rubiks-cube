{
  "name": "rubiks-cube-web",
  "version": "1.0.0",
  "description": "WebAssembly-powered 3D Rubik's Cube",
  "type": "module",
  "scripts": {
    "setup": "npm install && wasm-pack build --target web --out-dir pkg",
    "dev": "vite",
    "build": "wasm-pack build --target web --out-dir pkg && vite build",
    "preview": "vite preview",
    "wasm-build": "wasm-pack build --target web --out-dir pkg"
  },
  "dependencies": {
    "three": "^0.158.0"
  },
  "devDependencies": {
    "vite": "^5.0.8",
    "vite-plugin-wasm": "^3.2.2",
    "vite-plugin-top-level-await": "^1.3.1"
  }
}

